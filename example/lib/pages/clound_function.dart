import 'package:flutter/material.dart';

class CloundFunctionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('调用云函数')),
      body: Center(child: Text('调用云函数')),
    );
  }
}
